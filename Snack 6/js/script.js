// Crea un array di numeri interi e fai la somma di tutti gli elementi che sono in posizione dispari


const nums = [1, 3, 2, 9, 8]

for (let index = 0; index < nums.length; index++) {
    if (index % === 1) {
        somma += nums[index];
    }
    
}

console.log(somma)
